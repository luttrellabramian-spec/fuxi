# gRPC
grpcio>=1.62.0,<2.0.0
grpcio-tools>=1.62.0,<2.0.0

# LLM
openai>=1.30.0,<2.0.0

# 环境变量
python-dotenv>=1.0.0,<2.0.0

# 网络请求（web_tools / search_tools）
requests>=2.31.0,<3.0.0

# 配置文件
pyyaml>=6.0,<7.0

# 向量嵌入（cold_memory，向量搜索）
# 可选：未安装时 cold_memory 自动降级为纯文本搜索
sentence-transformers>=2.2.2,<4.0.0
numpy>=1.26.0,<2.0.0

# 测试依赖（可选）
pytest>=7.0.0,<8.0.0
